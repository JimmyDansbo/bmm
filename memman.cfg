FEATURES {
	STARTADDRESS: default = $A000;
}

MEMORY {
	BANKHEAD:	start = $A000, size = $24;
	HIRAM:		start = $A024, size = $1FDC;
}

SEGMENTS {
	HEADER:		load = BANKHEAD,	type = ro;
	JUMPTABLE:	load = HIRAM,		type = ro;
	MEMMAN:		load = HIRAM,		type = ro;
	LOWRAM:		load = HIRAM,		type = ro,	define = yes;
}
